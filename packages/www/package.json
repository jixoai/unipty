{
  "name": "@unipty/www",
  "version": "0.0.0",
  "private": true,
  "description": "Private static official documentation site for UniPty; deploys to GitHub Pages and consumes exactly one release catalog artifact.",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node scripts/check-site.mjs"
  }
}
