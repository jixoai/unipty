// terminal-header — pure barrel (tabs precedent): default = the
// canonical main; export * carries module-level named exports/types.
// The header is chrome-only (composition-first-apis, 2026-08-25): no
// sub-parts — the nav composes from the navigation-menu family.
export { default } from './terminal-header.svelte';
export * from './terminal-header.svelte';
