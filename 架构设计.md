# UniPty 架构设计

> [English README](README.md) · [简体中文 README](README-zh.md) · [能力规格（权威需求）](openspec/specs)
>
> 本文是仓库级架构导览：模块划分、关键接缝、数据面与生命周期法则、证据链设计。
> 行为级需求的唯一权威是 `openspec/specs/` 下的六份能力规格；本文与实现同源，帮助开发者建立整体认知。

## 1. 问题与目标

Node、Bun、Deno 三家的 PTY 底层各不相同：安装模型（npm 原生插件 / 运行时内置 / FFI + 动态库）、
I/O 表示（Buffer / 回调字节 / CString）、生命周期语义（destroy 是否杀进程、exit 如何观测）、部署约束
（预构建二进制 / 版本下限 / 权限旗标）互不兼容，消费项目被迫重复探测与适配。

UniPty 的目标：**一套小而诚实的运行时无关契约** + **可替换的 Backend 接缝**。应用显式选择 Backend；
核心绝不隐式探测、绝不静默切换语义。v1 范围刻意收敛在 PTY 本身——持久会话、重连、远程执行属于
Backend/wrapper 的显式扩展，不进入共同保证，也不引入第二套插件生命周期。

## 2. 工作区拓扑

```text
packages/
  unipty                        公共 Core（运行时中立，零 Backend 依赖）
  backend                       @unipty/backend 获取便利层（依赖 unipty）
  helper-backend                @unipty/helper-backend manifest 生成器（依赖 backend）
  backend-node-pty              官方 Node 路由（依赖 unipty + @lydell/node-pty）
  backend-bun                   官方 Bun 路由（依赖 unipty；底层为运行时内置）
  backend-deno-sigma__pty-ffi   官方 Deno 路由（依赖 unipty；vendor 自包含）
  conformance                   私有一致性装置 + 证据 + 目录（不发布）
  www                           私有静态文档站（消费发布目录，不发布）
  example                       私有本地演示（多运行时 WebSocket 终端）
```

依赖方向由 `scripts/check-architecture.sh.ts`（`pnpm check:arch`）强制：
Core 不依赖任何 Backend；Backend 只依赖 Core + 自身基底；helper 只依赖获取层；
conformance 与 www 不被任何包依赖；www 不依赖任何运行时包。

## 3. Core 与 Endpoint 接缝

**所有权法则**：公共 `Pty` 的全部可观测行为归 Core；Backend Endpoint 只提供原生传输事实。

```text
UniPty<TBackend>（配置实例，持有一个就绪 Backend）
  └─ spawn(argv, options) ──同步──▶ Pty（公共面）
        │ Core 私有持有
        ▼
     BackendEndpoint
        ├─ native: { input, output }            声明接受/产出的原生表示
        ├─ output: ReadableStream<NativeChunk>  一个有序源，仅 Core 消费
        ├─ exited: Promise<BackendExitResult>   可重复 await，独立于 EOF/取消/close
        ├─ write(NativeInput): boolean          布尔就绪；饱和整值拒绝
        ├─ drain(): Promise<void>               就绪恢复（非物理冲刷）
        ├─ resize(cols, rows): void             字符单元格；不支持显式失败
        └─ close(): void / terminate(): void    幂等、同步、非级联
```

关键点：

- **Backend 必须先就绪再注入**。原生加载、连接、协商都发生在 `new UniPty({ backend })` 之前
  （官方工厂 `await createXxxBackend()`），因此 `spawn()` 保持同步。
- **输出泵归 Core**。一个 reader 贯穿 PTY 生命周期；公共流取消只脱离视图，绝不取消私有源。
  首视图之前有有界 bootstrap 缓冲（满载即暂停拉取 = 输出背压，绝不截断）；全部视图脱离后排空丢弃；
  新视图只见订阅后的输出。
- **表示诚实**。`NativeChunk` 显式打标签（bytes / text / bytes+text）；文本永不被重编码冒充原生字节；
  `stream({ encoding: "bytes" })` 在纯文本端点上显式 `unsupported`。
- **生命周期非级联**。`close()` 先发布 closed 再做物理关闭，绝不杀子进程；`terminate()` 只是终止请求，
  绝不关传输；已建立的 `exited` 观察在两者之后都存活。物理清理可以异步完成（Deno 路由借此把
  `pty_close` 推迟到子进程退出之后——该原语天生 kill+close 一体）。
- **能力 token**。`pty.capability(token)` 按对象身份匹配，无字符串注册表；重复加载的包副本天然不互通。

## 4. 数据面

```text
公共写入 string ─┬─ 端点接受文本 ──▶ { kind: "text" }
                └─ 字节原生端点 ──▶ UTF-8 编码 ▶ { kind: "bytes" }
公共写入 Uint8Array ─┬─ 端点接受字节 ──▶ { kind: "bytes" }
                    └─ 严格文本端点 ──▶ unsupported（除非 Backend 显式开启 writeDecode）

输出 bytes ─┬─ utf8 视图：优先原生 text；否则增量解码（跨分块多字节安全）
            └─ bytes 视图：仅原生字节
输出 text  ──── utf8 视图直通；bytes 视图显式失败
```

写入就绪是**布尔**且**整值**：`true`/`false` 都表示完整接受（`false` 只是建议 `drain()`，绝不要求重试）；
饱和时以 `backpressure` 同步拒绝整个值。数值队列策略归各 Backend（Bun 1 MiB 硬界 + 3/4 软水位；
Node 1 MiB 准入队列；Deno 256 KiB/1 MiB 窗口），核心只标准化就绪/排空/饱和三种可观测结果。

## 5. 获取流水线（@unipty/backend）

```text
resolveUniPtyBackend(pkg, { from })   纯定位：不导入、不输出（node:module createRequire 适配三运行时）
inspectUniPtyBackend(resolution)      仅元数据导入 + 校验（schema/protocol/targets 预筛）
autoResolveUniPtyBackend(options)     显式候选有序择一；缺失则 package.json 回退（唯一否则 ambiguous）
                                      选定后：导入 → 工厂导出 → 工厂调用 → 就绪；任一失败均为终止性
                                      结构化 backend-initialization 错误（保留包/阶段/检查报告/cause）
```

打包部署改走显式 manifest（`defineUniPtyBackendManifest()` 校验并快照条目，loader 绝不提前执行），
由 `unipty-helper-backend manifest` 生成可手写的 ESM/TS 模块。元数据协议（`./unipty.metadata`，
schema 1）只承载身份/工厂/协议/目标/来源，绝不含成熟度、验证、能力或资产声明。

## 6. 一致性与证据链

```text
已安装公共包（pack → 隔离消费者安装 → 仅经公共导出驱动）
  └─ 一致性 profile（25 场景 × 三路由；要求↔场景映射可校验）
       └─ 全量通过 ──▶ 单条正向 Verification Evidence（包/运行时/元组/套件/commit/时间）
       └─ 失败/跳过   ──▶ 无记录（失败是 CI 诊断，绝不是永久 unsupported 声明）
            ▼
     确定性聚合（身份/元组/commit/唯一性/三路由覆盖校验）──▶ 发布目录 catalog
            ▼                                              ▼
     tag 发布（Trusted Publishing，npm-publish 环境）    www 站点原样消费，三态呈现
```

呈现只有三态：`verified`（精确证据存在）/ `declared-unverified`（声明匹配但无证据）/
`not-targeted`（声明未覆盖）；绝不放宽版本区间、绝不合并历史。发布门要求 Node/Bun/Deno
三条官方路由各至少一个原生通过元组；Deno 路由额外要求 packed-npm 制品在隔离消费者中通过
（拒绝运行时 `jsr:` 说明符）。

## 7. 官方路由的基底真相

| 路由                                  | 底层                                       | 关键真相（适配器如实映射）                                                                                                                                                                                       |
| ------------------------------------- | ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `@unipty/backend-node-pty`            | 第三方 node-pty（@lydell 预构建发行版）    | 上游 1.1.0 预构建在 darwin-arm64/Node22 上 posix_spawnp 失败；`destroy()` 会 SIGHUP 子进程 → 适配器直接销毁主 socket 实现纯传输关闭；exec 失败表现为退出观察而非 spawn 异常                                      |
| `@unipty/backend-bun`                 | 运行时原生 Bun.Terminal                    | `exit` 回调只报传输拆除 → 子进程退出时适配器合成主侧 EOF；`terminal.write` 的返回值是冲刷数而非部分接受（按整值交接）；输出无传输级流控（README 记录的底层限制）                                                 |
| `@unipty/backend-deno-sigma__pty-ffi` | 第三方 @sigma/pty-ffi（Rust portable-pty） | `pty_close` = kill+close 一体 → close 延迟物理化、terminate 按 pid 发信号（pgrep 差集发现）；信号致死统一报退出码 1（signal 恒 null，绝不伪造）；vendored 闭包打包为纯 JS（Deno 拒绝在 node_modules 下剥离类型） |

## 8. 扩展点

- **Backend wrapper**：包装另一个 Backend 或终端宿主（tmux/SSH/Docker……）的 Backend，满足同一
  就绪契约即可；无法执行公共操作时以 `unsupported` 显式报告。
- **能力 token**：Backend 包导出稳定单例 token + 品牌化类型；信号控制等 Backend 专属面经
  `pty.capability(token)` 暴露，核心视载荷为不透明。

明确不做（v1）：核心持久会话/重连、第二插件注册表、隐式 shell 求值、通用信号 API、
通用资产报告/复制器/`./unipty.build` 协议。
