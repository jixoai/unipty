# UniPty 贡献规范

> [English README](README.md) · [简体中文 README](README-zh.md) · [架构设计](架构设计.md)
>
> Issue / 讨论：<https://github.com/jixoai/unipty/issues>

## 开发环境

| 工具 | 版本             | 用途                              |
| ---- | ---------------- | --------------------------------- |
| Node | ≥ 22.20          | Core/获取层/Node 路由的构建与测试 |
| Bun  | ≥ 1.3.14         | Bun 路由套件、example 服务        |
| Deno | ≥ 2.9            | Deno 路由套件、vendoring 脚本     |
| pnpm | 10.x（corepack） | workspace 安装与脚本              |

```sh
git clone https://github.com/jixoai/unipty.git
cd unipty
corepack pnpm install
corepack pnpm build && corepack pnpm typecheck && corepack pnpm test
```

## 变更流程（OpenSpec 驱动）

本仓库用 [OpenSpec](https://openspec.dev) 管理需求，行为契约的变更必须先改规格：

1. **规格先行**：在 `openspec/changes/<change-id>/` 新建 proposal / design / specs / tasks，
   `openspec validate <change-id>` 通过后提交独立的 spec commit（`docs(spec): …`）。
2. **实现跟随**：代码 + 测试 + 对应 `tasks.md` 勾选在同一个实现 commit 中
   （`feat:` / `fix:` / `refactor:` / `test:`），保持原子可回滚。
3. **归档收尾**：全部任务完成并验证后 `openspec archive <change-id>`，独立归档 commit。
4. 实现期间发现规格无法满足（如底层真相与契约冲突）：**先改规格并记录原因**，再继续实现——
   绝不让实现悄悄偏离 `openspec/specs/`。

## 提交约定

- 遵循现有历史的 conventional 前缀（`feat:` / `fix:` / `docs:` / `test:` / `refactor:` /
  `ci:` / `build:`）， scopes 用包名或 `spec`。
- 按**功能目标**而非文件类型分组；过大的目标拆成多个可审查提交。
- spec 提交、实现提交、归档提交三类绝不混在同一 commit。

## 代码标准

- TypeScript 严格模式；**禁止** `any` / `as any` / `@ts-nocheck`（不可避免的第三方边界除外）。
- 每个源文件顶部维护「正交意图」注释：意图列表 + 原始需求 + 时间戳；3 个意图即考虑拆分。
- 相对导入使用 `.ts` 扩展名；注释语言与所在文件既有注释一致（仓库以英文为主）。
- 依赖方向必须通过 `pnpm check:arch`（见 [架构设计](架构设计.md#2-工作区拓扑)）。

## 测试期望

| 包                                  | 命令                                                                       | 内容                                          |
| ----------------------------------- | -------------------------------------------------------------------------- | --------------------------------------------- |
| unipty                              | `pnpm --filter unipty test`                                                | 内存 Endpoint 上的契约单测（当前 102 个）     |
| @unipty/backend                     | `pnpm --filter @unipty/backend test`                                       | 解析/检查/autoResolve/manifest + fixture 包   |
| @unipty/helper-backend              | `pnpm --filter @unipty/helper-backend test`                                | 生成契约 + CLI 法则                           |
| @unipty/backend-node-pty            | `pnpm --filter @unipty/backend-node-pty test`                              | 真实 PTY 适配器测试                           |
| @unipty/backend-bun                 | `cd packages/backend-bun && bun test`                                      | 真实 PTY（需 Bun）                            |
| @unipty/backend-deno-sigma__pty-ffi | `cd packages/backend-deno-sigma__pty-ffi && deno test -A --no-check test/` | 真实 FFI PTY（需 Deno，离线跑 vendored 资产） |
| @unipty/conformance                 | `pnpm --filter @unipty/conformance test`                                   | 报告/证据/目录/追溯                           |

- **native 支持声明只有一条验收缝**：installed-public-package 一致性套件。适配器内部测试与 mock
  只做本地诊断，永远不能证明原生支持。
- 一致性 fixture 是确定性的跨运行时程序（`.mjs`，仅 `process.*` 全局）；oracle 做了 tty 现实归一化
  （ONLCR / ANSI 进度噪声）——先读 `packages/conformance/src/profile/util.ts` 再加断言。
- 提交前全绿：`pnpm fmt:check && pnpm check:arch && pnpm typecheck && pnpm test`。

## 文档规范

- **双语**：根 README（英文主）+ README-zh.md；**每个 npm 包都有自己独立的
  README.md 与 README-zh.md**——面向 npm 使用者自包含（禁止 `../../README-zh.md` 之类的仓库内交叉
  导航当作正文），并在 `files` 中一并发布。
- 子包 README 必须如实陈述底层真相与支持状态（verified 只来自发布目录证据）。
- 领域词汇以 `i18n.zh.md` 为准；架构结论沉淀进 `AGENTS.md` 与 [架构设计](架构设计.md)。

## 发布流程（维护者）

发布是证据门控的，不接受手工绕过：

1. push 到 main 触发 CI：quality / bun-suite / deno-suite / conformance-matrix
   （矩阵对已安装制品跑全量 profile，仅在**全部通过**时上传证据制品）。
2. 推 `v*` tag 触发 `release.yml`：
   - `verify-route-coverage`：收集同 commit 的全部证据 → 聚合目录（校验身份/元组/唯一性/三路由覆盖）；
   - `publish`：在 `npm-publish` environment 下经 **npm Trusted Publishing**（OIDC，无库存令牌）
     按依赖序发布六个包，并把目录附到同一 release。
3. 站点部署独立可重试：`deploy-www.yml`（workflow_dispatch + release tag）消费该 release 的
   目录制品原样渲染，绝不影响包发布。CNAME/DNS（unipty.jixoai.com）由 Owner 管理。

版本策略：workspace 内 `workspace:*`，发布版本在各包 `package.json` 中显式 bump；
新版本必须伴随新的验证证据（证据与包版本精确绑定，不继承）。
